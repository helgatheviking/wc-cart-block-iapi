# Cart Block with Interactivity API

## Quickstart

This is an experimental repo. Clone this repo and run `composer install && npm install && npm run build`   

### What's This?

Experimental plugin that registers a block pattern for a cart block that uses inner blocks and interactivity.

>**Warning**

1. This is provided _as is_ and is for exploratory/learning purposes only.